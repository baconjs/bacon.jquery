## BJB Example App with plain script tags

You must run [Bower](https://github.com/bower/bower) to fetch dependencies before opening the example app.

    npm install -g bower
    bower install
    open index.html
